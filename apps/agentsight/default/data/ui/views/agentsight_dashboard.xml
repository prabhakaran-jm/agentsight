<dashboard version="1.1" theme="dark">
  <label>AgentSight</label>
  <description>Splunk watches the agents that use Splunk — observability for MCP clients and autonomous agents.</description>

  <fieldset submitButton="false" autoRun="true">
    <input type="time" token="time_range">
      <label>Time range</label>
      <default>
        <earliest>-30m@m</earliest>
        <latest>now</latest>
      </default>
    </input>
  </fieldset>

  <row>
    <panel>
      <title>MCP Activity Timeline</title>
      <chart>
        <search>
          <query>index=_internal sourcetype=mcp_server rpc_method=tools/call
| spath
| timechart span=1m count by tool_name</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
          <refresh>30s</refresh>
          <refreshType>delay</refreshType>
        </search>
        <option name="charting.chart">column</option>
        <option name="charting.legend.placement">bottom</option>
      </chart>
    </panel>
  </row>

  <row>
    <panel>
      <title>Recent MCP Tool Calls</title>
      <table>
        <search>
          <query>index=_internal sourcetype=mcp_server rpc_method=tools/call
| spath
| eval duration_ms=round(execution_time_seconds*1000,0)
| table _time username tool_name status duration_ms request_id source_ip
| sort - _time
| head 20</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
          <refresh>30s</refresh>
          <refreshType>delay</refreshType>
        </search>
      </table>
    </panel>
  </row>

  <row>
    <panel>
      <title>Active Cases</title>
      <table>
        <search>
          <query>index=agentsight sourcetype=agentsight:case
| sort - _time
| dedup case_id
| table _time case_id trigger_rule severity status actor classification summary
| sort - _time</query>
          <earliest>-7d</earliest>
          <latest>now</latest>
          <refresh>60s</refresh>
        </search>
      </table>
    </panel>
    <panel>
      <title>Pending Agent Actions</title>
      <table>
        <search>
          <query>index=agentsight sourcetype=agentsight:case status=awaiting_approval
| sort - _time
| dedup case_id
| spath input=_raw path=pending_action_details{}
| table _time case_id actor trigger_rule status pending_actions
| sort - _time</query>
          <earliest>-7d</earliest>
          <latest>now</latest>
          <refresh>60s</refresh>
        </search>
      </table>
    </panel>
  </row>

  <fieldset submitButton="true" autoRun="false">
    <input type="text" token="approve_case_id">
      <label>Case ID</label>
      <default></default>
    </input>
    <input type="text" token="approve_action_id">
      <label>Action ID</label>
      <default></default>
    </input>
    <input type="dropdown" token="approve_decision">
      <label>Decision</label>
      <choice value="approved">Approve</choice>
      <choice value="denied">Deny</choice>
      <default>approved</default>
    </input>
  </fieldset>

  <row>
    <panel>
      <title>Approve / Deny Queued Action</title>
      <table>
        <search>
          <query>| agentsight_approve case_id="$approve_case_id$" action_id="$approve_action_id$" decision="$approve_decision$" actor="$env:user$"
| table _time status case_id action_id decision new_case_status followup_sid message</query>
          <earliest>-1m</earliest>
          <latest>now</latest>
        </search>
      </table>
      <html>
        <p>Enter <code>case_id</code> and <code>action_id</code> from a pending case, then change the decision dropdown to re-run.</p>
        <p>Try in Search: <code>index=agentsight sourcetype=agentsight:case case_id=YOUR_ID | agentsight_explain case_id=YOUR_ID</code></p>
      </html>
    </panel>
  </row>
</dashboard>
